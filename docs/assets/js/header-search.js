// Superseded by search.js — the custom search component now fully replaces
// Material's search (it injects its own header icon and hides Material's). This
// file is intentionally a no-op so nothing focuses Material's input (which used
// to jump the page to the top and open the stray Material popup).
